A Desktop Application that accepts an input from user to convert from one currency to another currency and displays converted the Currency Value.
This Application uses WPF for UI in FrontEnd and C# in Backend for Communication with UI and User inputs.
